"""08. Nodes at Given Distance (GFG, easy)."""

from tree import TreeNode


def k_distance(root: TreeNode | None, k: int) -> list[int]:
    """Values of nodes at distance k from the root, left to right."""
    raise NotImplementedError
